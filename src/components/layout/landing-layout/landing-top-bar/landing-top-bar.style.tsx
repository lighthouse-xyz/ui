import IconButton from "@mui/material/IconButton";
import { styled } from "@mui/material/styles";

const MoreIconButton = styled(IconButton)({
  borderRadius: "106px",
  color: "white",
  border: "1px solid #FFFFFF",
  width: "40px",
  height: "40px",
});

export { MoreIconButton };
